<%= simple_form_for @item, :url => @url do |f| %>

  <% content = capture do %>
    <%= f.input @columns.first.to_sym %>
  <% end %>

  <%= form_section :title => 'overview', :content => content %>

  <%= f.button :submit %>

<% end %>
